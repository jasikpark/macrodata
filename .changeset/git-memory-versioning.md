---
"@macrodata/opencode": minor
---

Version memory state with git after background skills. Dreamtime, memory-maintenance, and distill now commit the macrodata state directory at the end of each run, giving memory a complete history. Onboarding initializes the git repo with a `.gitignore` for runtime files as part of setup.
