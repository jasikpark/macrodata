{
  "name": "ambient-recall-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone spike: Qwen3-embedding ambient memory recall over macrodata markdown. Separate Qwen3/1024 index; reify into the plugin later.",
  "scripts": {
    "reindex": "bun run reindex.ts",
    "search": "bun run search-cli.ts"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.6.0",
    "vectra": "^0.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
